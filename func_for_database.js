function startDatabase(databaseConnection) {
    // detect if there is a database already

    // if there is...
    // say that there is on the console

    // if there isn't...
    // create the database and the tables
}

module.exports = {startDatabase};